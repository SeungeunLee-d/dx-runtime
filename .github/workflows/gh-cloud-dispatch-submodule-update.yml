name: dx-all-suite-dispatch-update
run-name: dispatch submodule update of dx-all-suite by ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches:
      - staging

env:
  OWNER: DEEPX-AI
  REPO: dx-all-suite

jobs:
  dispatch:
    runs-on:
      - self-hosted
      - sdk
    steps:
      - name: Dispatch Submodule Update of dx-all-suite
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.GC_DCI_TOKEN }}
          repository: ${{ env.OWNER }}/${{ env.REPO }}
          event-type: update-submodule
          client-payload: '{"name": "dx-runtime", "sha": "${{ github.sha }}", "ref": "${{ github.ref }}"}'
